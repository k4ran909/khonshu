module.exports = {
    token: "Your Discord user token here",
    prefix: "$",
    // Owner is required — index.js reads global.config.owner to gate all commands
    // to a single user. Get it via Discord → Settings → Advanced → Developer Mode →
    // right-click your name → "Copy User ID".
    owner: "Your Discord user ID here"
};

/* remove .example from the name of this file */
